import { analog } from '../../features/Sequencer/defaults/defaultSequences';
import { house } from '../../features/Sequencer/defaults/defaultSequences';

export const cloudUser = {
  username: 'cloudUsername',
  sequences: [analog],
};

export const idbUser = {
  username: 'idbUsername',
  sequences: [house],
};
